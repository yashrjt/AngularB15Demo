import { DescribeDirective } from './describe.directive';

describe('DescribeDirective', () => {
  it('should create an instance', () => {
    const directive = new DescribeDirective();
    expect(directive).toBeTruthy();
  });
});
